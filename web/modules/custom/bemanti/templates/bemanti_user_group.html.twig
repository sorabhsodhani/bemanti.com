{#
/**
 * @file bemanti_user_group.html.twig
 * Default theme implementation to present Bemanti user group entity data.
 *
 * This template is used when viewing Bemanti user group entity pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_bemanti_user_group()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('bemanti_user_group') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
