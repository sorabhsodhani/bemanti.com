{#
/**
 * @file bemanti_user_bet_entity.html.twig
 * Default theme implementation to present Bemanti user bet entity data.
 *
 * This template is used when viewing Bemanti user bet entity pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_bemanti_user_bet_entity()
 *
 * @ingroup themeable
 */
#}

{% set matches_list = get_upcoming_matches() %}
{% for i in matches_list %}
     {{ attribute(_context, 'match_form_'~i) }}
{% endfor %}